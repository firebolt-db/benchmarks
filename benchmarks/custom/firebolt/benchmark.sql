-- Custom Benchmark Queries for Firebolt
-- Replace these queries with your own custom queries to benchmark.
-- Each query should end with a semicolon (;).
-- Lines starting with -- are comments and will be ignored.

-- Example query 1: Simple SELECT
SELECT 1;

-- Example query 2: Replace with your custom query
-- SELECT * FROM your_table LIMIT 100;
