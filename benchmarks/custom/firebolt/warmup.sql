-- Custom Warmup Queries for Firebolt
-- Add any warmup queries here if needed.
-- These queries run before the benchmark to warm up caches.
-- Leave empty or with just a simple query if no warmup is needed.

SELECT 1;
